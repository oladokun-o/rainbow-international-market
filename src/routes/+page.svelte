<main class="wrap">
  <p class="eyebrow type-overline">San Angelo, TX</p>
  <h1 class="type-hero">Rainbow International Market</h1>
  <p class="type-body-lg tagline">Coming soon — African, Caribbean, Asian &amp; international groceries.</p>
  <span class="pill">Browse &middot; Reserve &middot; Cash on pickup</span>
</main>

<style>
  .wrap {
    min-height: 100dvh;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 1rem;
    padding: 2rem;
    text-align: center;
  }

  .eyebrow {
    color: var(--color-green);
  }

  h1 {
    color: var(--color-green);
    max-width: 16ch;
  }

  .tagline {
    color: var(--color-deep);
    max-width: 42ch;
  }

  .pill {
    margin-top: 0.5rem;
    padding: 0.5rem 1rem;
    border-radius: 999px;
    background: var(--color-amber);
    color: var(--color-deep);
    font-weight: 600;
    font-size: 0.875rem;
  }
</style>
